name: onedart
description: A sample command-line application.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.11.4

# Add regular dependencies here.
dependencies:
  path: ^1.9.0
  args: ^2.7.0
  win32: 5.5.4
  ffi: ^2.2.0
dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
bin:
  executables:
    onedart: onedart # This maps 'onedart' command to bin/onedart.dart
